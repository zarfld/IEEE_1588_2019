/**
 * @file credentials.example.h
 * @brief WiFi credentials template (SAFE FOR GITHUB)
 * 
 * This is an example template for WiFi credentials.
 * Copy this file to credentials.h and fill in your actual WiFi details.
 * 
 * INSTRUCTIONS:
 * 1. Copy this file: cp credentials.example.h credentials.h
 * 2. Edit credentials.h with your actual WiFi SSID and password
 * 3. credentials.h is gitignored and will NOT be committed
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Replace with your WiFi network name
const char* WIFI_SSID = "YOUR_WIFI_SSID";

// Replace with your WiFi password
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

#endif // CREDENTIALS_H
