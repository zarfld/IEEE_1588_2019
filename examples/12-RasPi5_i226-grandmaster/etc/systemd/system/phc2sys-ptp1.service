[Unit]
Description=Sync PHC /dev/ptp1 (eth0) from CLOCK_REALTIME
After=chrony.service
Wants=chrony.service

[Service]
Type=simple
ExecStart=/usr/sbin/phc2sys -s CLOCK_REALTIME -c /dev/ptp1 -O 0 -m
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target
